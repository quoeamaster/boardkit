VITE_DEBUG_MODE_ON=true
# VITE_DEBUG_MODE_ON=false

# for vite, the file stays at /public folder
# for production, the file stays in the server's public folder instead (could be an absolute path instead)
VITE_CONFIG_FILE_LOCATION=config.json

